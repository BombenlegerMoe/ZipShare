<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Global default: TLS only, system CAs only. Never flip cleartextTrafficPermitted here. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Cleartext is opened per-domain only, for local / self-hosted hostnames.
         A profile with allowCleartext=true ALSO needs its host listed here; the per-profile
         OkHttp client is the second gate (it omits ConnectionSpec.CLEARTEXT otherwise). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="true">local</domain>
        <domain includeSubdomains="true">lan</domain>
        <domain includeSubdomains="true">internal</domain>
        <domain includeSubdomains="true">home.arpa</domain>
    </domain-config>
</network-security-config>
